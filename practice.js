var bestName = bill; 

function sayMyName(myName){
    bestName=myName
    shareName(bestName)

}

sayMyName("Andy")

function shareName(params) {
    for (var index = 0; index < array.length; index++) {
        var element = array[index];
        if (condition) {
            
        }
    }
    function name2(params) {
        var A
        function name1(params) {
            
        }
    }
    agrees = prompt()
    return agrees
}

function name(params) {
    
}